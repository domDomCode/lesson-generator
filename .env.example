# Copy to .env for local development: `cp .env.example .env`

# "true" starts the in-browser MSW mock backend (mock-backend/).
# There is no real backend in this repo, so keep this on for local dev.
VITE_ENABLE_MOCKS=true
